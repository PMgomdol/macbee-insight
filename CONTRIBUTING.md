# 같이 작업하는 법 (운영진용)

맥비 자료실 코드를 고치고 사이트에 반영하는 흐름입니다. 개발 용어는 처음 나올 때 풀어 썼어요.

## 한눈에

```
브랜치 만들기 → 수정 → PR 올리기 → 재찬 확인·반영 → 사이트 자동 배포
```

- **main** = 지금 사이트에 올라가 있는 정본. **직접 고칠 수 없게 막혀 있어요** (실수 방지).
- **브랜치** = 정본을 건드리지 않는 내 작업용 복사본.
- **PR (Pull Request)** = "이거 정본에 반영해 주세요" 하는 검토 요청. 무엇이 바뀌었는지 비교 화면으로 보여줘요.
- 반영(머지)되면 Vercel이 **알아서 사이트에 올립니다.** 별도 배포 버튼 없음.

## 1. 처음 한 번

로컬에서 띄우는 법은 [web/README.md](web/README.md)를 보세요. 요약하면 `web/` 안에서 `.env.local` 채우고 `npm install` → `npm run dev`.

- `.env.local`에 들어갈 키 값은 **재찬에게 요청**하세요. 깃허브·카톡·슬랙에 올리지 마세요.
- Supabase 키만 있어도 화면은 뜹니다. 나머지(Gemini·GA4·PostHog·메일)는 그 기능을 만질 때만 필요해요.

## 2. 수정할 때마다

```bash
git checkout main
git pull                          # 최신 정본 받기
git checkout -b fix/검색창-높이    # 내 브랜치 만들기
# ... 수정 ...
git add -A
git commit -m "fix(search): 모바일 검색창 높이 정상화"
git push -u origin fix/검색창-높이
```

푸시하면 GitHub가 **"Compare & pull request" 버튼**을 보여줍니다. 누르고 아래만 채우면 돼요:
- 제목: 커밋 메시지와 같게
- 본문: 무엇을 왜 바꿨는지 한두 줄, 화면이 바뀌면 스크린샷

**브랜치 이름**은 `종류/내용` — `fix/…`(버그), `feat/…`(기능), `docs/…`(문서), `style/…`(디자인).

## 3. PR 올린 뒤

- 잠시 후 PR 화면에 Vercel **미리보기 링크**가 붙습니다. 정본을 건드리기 전에 실제 화면을 볼 수 있어요. (붙지 않으면 재찬에게 — 무료 플랜 제약일 수 있음)
- 재찬(또는 다른 운영진 1명)이 확인하고 반영합니다. 수정 요청이 오면 같은 브랜치에 커밋을 더 올리면 PR에 자동으로 반영돼요.
- 반영되면 1~2분 안에 [macbe-archive.com](https://macbe-archive.com)에 올라갑니다.

## 4. 지켜야 할 것

- **main에 직접 push 금지** — 막혀 있어서 되지도 않지만, 시도하지 마세요.
- **`.env.local`·키 값은 절대 커밋 금지** — `.gitignore`에 걸려 있지만 복사해서 다른 파일에 넣지 마세요. 이 레포는 **공개**라 누구나 볼 수 있어요.
- **DB 데이터를 직접 바꾸는 작업**(초기화·대량 수정·삭제)은 코드 PR과 다릅니다. [web/AGENTS.md](web/AGENTS.md)의 "데이터 작업 규칙"을 먼저 보고, 재찬과 상의 후 진행하세요.
- 커밋 메시지는 `종류(범위): 한글 설명` — 예: `fix(card): 드라이브 파일 라벨 복귀`. 기존 커밋 로그를 보면 감이 옵니다.
- UI를 만질 땐 [web/AGENTS.md](web/AGENTS.md)의 "UI 일관성 규칙"(같은 모양 = 같은 역할, 컴포넌트 기준은 `/design` 페이지)을 따라 주세요.

## 5. 막히면

- 배포 실패(PR에 빨간 X): 상세 로그는 Vercel 안에서만 보여서 **재찬에게** 말씀 주세요.
- git 충돌: `git pull origin main` 후 충돌 파일 정리 → 다시 커밋·푸시. 어려우면 그대로 두고 물어보세요, 같이 봅니다.
- 그 외: 운영진 카톡방.
