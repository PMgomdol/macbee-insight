# UX/UI 전체 감사 결과 — 2026-07-09

3개 영역 병렬 검토 (홈·검색·리스트 / 등록·승인·FAQ / 공통 컴포넌트·접근성).
발견 이슈 검증 후 오탐 1건 제외(캐러셀 data-card — 실제로는 정상 작동), 중복 통합.

**진행 현황 (2026-07-09 업데이트):**
- ✅ 수정 완료 4건: C2 (FAQ 접기) · C3 (승인 원자화 — RPC 배포 + 풀사이클 검증) · C5 (문장형 검색 토큰화) · C6 (필터 클라이언트화)
- ⬜ 미수정 38건: C1 · C4 (치명 2) + 높음 9 + 중간 15 + 낮음 12

---

## 🔴 치명 (기능이 사실상 안 됨 — 6건)

| # | 위치 | 문제 | 제안 |
|---|---|---|---|
| C1 | SubmitForm.tsx:46,77 | **URL 자동분석 실패 시 등록 자체가 불가능** — 상세 입력 필드가 분석 성공 후에만 열림. 브런치·페이월·JS렌더 사이트는 분석 실패 → 수동 입력 경로 없음 | 분석 실패 시 "직접 입력하기" 폴백 |
| ~~C2~~ ✅ | FaqList.tsx:16-21 | ~~**FAQ '더 보기' 접기가 실제론 미작동**~~ 수정됨 — details toggle + ResizeObserver 재측정 — 닫힌 `<details>` 안에서 높이 측정(scrollHeight=0) → 항상 전체 노출. 어제 만든 기능이 안 돌아감 (검증 완료) | `<details>` open 이벤트 시 재측정 |
| ~~C3~~ ✅ | admin/actions.ts:57-70 | ~~**2인 승인 동시성 버그**~~ 수정됨 — approve_proposal_atomic RPC (FOR UPDATE), 중복·동시·사후 승인 케이스 검증 통과 — 두 운영진이 거의 동시 승인 시 마지막 쓰기가 이전 승인 덮어씀 → 승인 유실 | DB 원자적 배열 append (RPC) |
| C4 | actions.ts:311 | **이메일 약속 미이행** — "결과는 이메일로 알려드릴게요" UI 문구는 있는데 발송 코드가 어디에도 없음 | 발송 구현 or 문구 수정. 회의 액션(노티)과 묶어 처리 |
| ~~C5~~ ✅ | lib/search.ts:34-52 | ~~**문장형 검색 미지원**~~ 수정됨 — 어절 토큰화(불용어·조사 처리), 가중치 원질의 10 > 토큰 7 > 동의어 5 — 홈 placeholder가 "회원가입 정책이 궁금해요" 같은 문장을 유도하는데, 엔진은 전체 문자열 substring 매칭이라 문장 입력 시 0건. 기대와 능력 불일치 | 어절 토큰화 후 AND/OR 매칭 (또는 placeholder를 키워드형으로) |
| ~~C6~~ ✅ | search/page.tsx + loading.tsx | ~~**필터 클릭마다 전체 화면 스켈레톤 깜빡임**~~ 수정됨 — SearchResultsClient 신설, 필터·정렬 클라이언트 상태 + URL replaceState — 서버 렌더 구조라 kind/카테고리/정렬 클릭할 때마다 방금 조작한 칩까지 통째로 사라졌다 다시 그려짐 | 필터를 클라이언트 상태로 (홈 예전 AllItemsSection 패턴) |

## 🟠 높음 (사용성 큰 마찰 — 9건)

| # | 위치 | 문제 |
|---|---|---|
| H1 | layout.tsx:74 | Skip link 부재 — 키보드 사용자 매 페이지 헤더 전체 통과 |
| H2 | MobileNav.tsx:59 | 모바일 메뉴: 포커스 트랩·Esc 닫기·aria-modal 없음 |
| H3 | SearchAutocomplete | 자동완성 combobox ARIA 불완전 (aria-controls·activedescendant 없음) — 스크린리더가 활성 항목 못 읽음 |
| H4 | SearchAutocomplete:154 | **모바일에서 헤더 검색 진입점 없음** (hidden sm:flex) — 홈 외 페이지에서 검색하려면 햄버거 열어야 |
| H5 | ItemCard:132 | 새탭/다운로드 아이콘이 hover에만 노출 — 터치 기기에선 영영 안 보임 |
| H6 | globals.css | 다크모드 브랜드 버튼 대비 2.75:1 (WCAG 미달) |
| H7 | SubmitForm 다수 | 폼 라벨-입력 id 연결 누락 (7개 필드) + 완료 모달 포커스 트랩 없음 |
| H8 | search/page.tsx:79 | 동의어 배너 링크가 질의를 **교체** — 클릭하면 원 검색어 잃고 결과가 오히려 줄어듦 |
| H9 | HorizontalScroll:36 | 캐러셀 드래그 후 click 억제 없음 — 드래그하다 카드 위에서 놓으면 의도치 않은 새 탭 |

## 🟡 중간 (개선 필요 — 15건)

| # | 위치 | 문제 |
|---|---|---|
| M1 | ListFilterClient:193 | 리스트 빈 결과에 "필터 초기화" CTA 없음 (검색 페이지와 불일치) |
| M2 | ListFilterClient:50 | 리스트 내 검색이 동의어 미적용 — 홈 검색과 규칙 다름 |
| M3 | 3개 검색 UI | 자동완성 유/무·모양·동작 제각각 (홈/리스트/FAQ) |
| M4 | active 칩 스타일 | 대분류=accent 채움 vs kind·정렬·소분류=연회색 — 선택 상태 판독 어려움 |
| M5 | 다수 | 터치 타겟 44px 미달: 필터 칩(~25px)·햄버거(36px)·테마 토글(28~32px)·X 버튼 |
| M6 | SearchAutocomplete:97 | suggest fetch 레이스 가드 없음 — 늦게 온 응답이 최신 결과 덮어씀 |
| M7 | SearchAutocomplete:60 | URL q 변경 시 입력창 미동기화 (동의어 링크 클릭 후 입력창에 옛 질의) |
| M8 | SearchAutocomplete:74 | placeholder 2.6s 자동 순환 — WCAG 2.2.2(정지 수단 없는 움직임) |
| M9 | search/page.tsx:200 | 자료 0건+FAQ 1건이면 "못 찾았어요" 안내 안 뜸 |
| M10 | SubmitForm:294 | 파일 input accept 속성 없음 + 업로드 진행률 없음 (50MB 허용인데 스피너만) |
| M11 | actions.ts:341 | Supabase 원본 에러 메시지 사용자 노출 |
| M12 | admin | 검토 큐에 승인자 이름 미표시 (n/2 카운트만) + 제안자=승인자 방지 없음 |
| M13 | buttons.tsx:34 | 승인/거절이 window.prompt/confirm 의존 — 모바일 UX·접근성 저하 |
| M14 | admin/page:188 | 단독 승인 조건(운영진<2명)을 서버가 검증 안 함 — UI 가드뿐 |
| M15 | MobileNav:83 | 모바일 nav 활성 판정 정확 일치만 + aria-current 없음 (데스크톱과 로직 불일치) |

## 🟢 낮음 (다듬기 — 대표 12건)

- 검색: Escape가 input blur까지 시킴 / 활성 항목 scrollIntoView 없음 / 드롭다운 행별 동작 3종인데 예고 없음 / 카운트 단위 "N" vs "N건" 불일치
- 홈: "전체 자료 →"가 /insights로만 감 (라벨과 불일치) / "사람들이 많이 보고 있어요" 문구 vs 월간 데이터 뉘앙스
- 카드: 배지 "파일"+다운로드 아이콘인데 실제론 뷰어 열기 / 스켈레톤 높이 148px vs 실제 180px (CLS)
- 폼: date input에 무의미한 placeholder / 같은 파일 재선택 무반응 / URL 스킴 자동 보정 없음 / 글자수 카운터 없음
- 리스트: insights 693건 일괄 전송 (초기 페이로드) / "더 보기" 진행률 없음
- 기타: ListSearchBox 죽은 코드 / checkDuplicate 죽은 export / 푸터 h3 heading 레벨 점프 / 외부링크 아이콘 10px 과소

---

## 추천 처리 순서 (남은 것 기준)

1. **C1 (등록 폴백) · C4 (이메일/노티)** — 남은 치명 2건. C4는 회의 액션 '신규 등록 노티'·'승인 시 제안자 메일'과 같은 작업으로 묶어 처리
2. **H4·H5·H8·H9** — 실사용 마찰 즉효성 높은 것 (모바일 검색 진입점·hover 아이콘·동의어 링크·캐러셀 드래그 클릭)
3. 접근성 묶음 (H1·H2·H3·H7 + M5 터치 타겟) — 한 번에 처리 효율적
4. 일관성 묶음 (M1~M4) — 검색/필터 UX 통일. 단 M1·M9는 C6 수정으로 검색 페이지 쪽은 해소됨 — 리스트 페이지(/files·/insights)만 남음
5. 낮음은 다른 작업 하는 김에

## 수정 이력

- 2026-07-09: C2·C3·C5·C6 수정 (커밋 8f5a22a). C3는 approve_proposal_atomic RPC 생성 후 테스트 행으로 승인 1인→중복→2인 도달→사후 시도 전 케이스 검증.
